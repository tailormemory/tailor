# TAILOR — Python dependencies
# Core
chromadb==1.5.9
mcp>=1.0
uvicorn>=0.30
starlette>=0.40
sse-starlette>=3.0
anyio>=4.0
httpx>=0.28

# Embedding & search
numpy>=2.0
onnxruntime>=1.18
tokenizers>=0.20

# Document processing
openpyxl>=3.1           # Excel
xlrd>=2.0               # Legacy .xls
python-docx>=1.0        # Word
python-pptx>=1.0        # PowerPoint
PyMuPDF>=1.27           # PDF
pillow>=10.0            # Images / OCR fallback
pyyaml>=6.0             # Config

# OCR
paddlepaddle>=3.0
paddleocr>=3.0
paddlex[ocr]>=3.5

# API & networking
requests>=2.30
aiohttp>=3.9

# Services
filelock>=3.12          # Telegram bot session locking

# Gmail sync (optional — only if using Gmail connector)
google-api-python-client>=2.100
google-auth>=2.20
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2

# macOS only (Apple Vision OCR fallback in ingest pipeline)
pyobjc-core>=12.0
pyobjc-framework-Cocoa>=12.0
pyobjc-framework-Quartz>=12.0
pyobjc-framework-Vision>=12.0
